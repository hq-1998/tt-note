import Validate from './validate'
import globalStorage from './storage'
import { generateNote } from './note'
import { shallowMergeObject, getImageBase64, loadSvg } from './tool'

export { Validate, globalStorage, generateNote, shallowMergeObject, getImageBase64, loadSvg }
